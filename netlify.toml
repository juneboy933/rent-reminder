[build]
  base    = "Frontend"
  command = "npm install && npm run build"
  publish = ".next"  # relative to the base directory (or "out" if you use `next export`)

# optionally include plugin for optimized Next.js builds
# [[plugins]]
#   package = "@netlify/plugin-nextjs"