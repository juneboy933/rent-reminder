# Frontend (Next.js)

This frontend now uses Next.js App Router.

## Run

```bash
cd Frontend
npm install
npm run dev
```

Open http://localhost:3000

If your backend is on another URL, set API base in the Login page before signing in.
Default API base is `http://localhost:3000/api`.


